# RUNBOOK — release day 2026-09-11 (IPCA + CPI-US + PCE)

Sequencial. Não pula etapa. Todos os comandos rodam em máquina corp com
`opt_utils` disponível.

## Ordem cronológica

| Horário BRT | Evento | Ação |
|---|---|---|
| Antes das 09:00 | prep | `git pull` em `python_tools` (puxa até `95eaaad`) |
| 09:00 | IPCA Ago release (IBGE) | Bloco A abaixo |
| 09:30 | CPI-US Ago release (BLS) | Bloco B abaixo |
| durante o dia | apresentação PCE difusão | Bloco C abaixo |

---

## Bloco A — IPCA (após release IBGE ~09:00)

**Path rápido** (recomendado pra release day): 21 cats do dashboard, `--no-bcb`, ~2-3 min. Segue passo-a-passo em `pareto_ipca/script_itau/RUNBOOK_ATUALIZACAO_DASHBOARD.md`:

```bash
cd pareto_ipca
Rscript scripts/reconstruct_ipca.R --no-bcb           # ~10-15s
Rscript scripts/build_pareto_indice.R                 # <5s

# Load NSA (21 cats)
python script_itau/load_pareto_to_sql.py --only total,alim_e_bebidas,alim_domicilio,alim_fora,habitacao,energia_eletrica,artigos_residencia,vestuario,transportes,passagem_aerea,auto_novo,auto_usado,gasolina,saude,higiene_pessoal,despesas_pessoais,educacao,comunicacao,administrados,industriais,servicos

# Load SA (X-13 + fix servicos)
python script_itau/load_pareto_to_sql.py --only total,alim_e_bebidas,alim_domicilio,alim_fora,habitacao,energia_eletrica,artigos_residencia,vestuario,transportes,passagem_aerea,auto_novo,auto_usado,gasolina,saude,higiene_pessoal,despesas_pessoais,educacao,comunicacao,administrados,industriais,servicos --sa
python script_itau/_fix_servicos_idx_sa.py            # fix TD peak

# IPCA-15 (idem se quiser reprocessar — não tem release novo hoje):
cd ../pareto_ipca15
Rscript scripts/reconstruct_ipca.R --no-bcb
Rscript scripts/build_pareto_indice.R
# Segue RUNBOOK_ATUALIZACAO_21_CATS.md (mesmo escopo)
```

**Path lento** (só se precisar full 44 cats + núcleos): `Rscript scripts/seed_ibge_history.R` (rebuild histórico completo ~50s) + `load_pareto_to_sql.py --sa` (sem `--only`) + fixes DP/medio SA (`_sa_dp_nucleo_medio.R` + `_fix_dp_medio_sa.py`). Ver `pareto_ipca/script_itau/RUNBOOK.md` Estágio 5.1.

**Sanity checks pós-IPCA**:
- Difusão idx: `IPCA:difusao` deve mostrar valor 30-80 (não 10^50). Se der overflow, o fix não puxou — confirma commit `300f6e6` está no branch.
- Total: peso deve ser 100.0 constante em todo o histórico.
- **Rows órfãs no corp** (não deleta automático, cleanup manual sob demanda):
  ```sql
  SELECT * FROM OPT_Macro_Series_2 WHERE haver_code LIKE 'PARETO_IPCA:ipca_total%'
                                      OR haver_code LIKE 'PARETO_IPCA:ex3_industriais%'
                                      OR haver_code LIKE 'PARETO_IPCA:ex3_servicos%'
                                      OR haver_code LIKE 'PARETO_IPCA:serv_subj_estrito%'
                                      OR haver_code LIKE 'PARETO_IPCA:automovel_novo%'
                                      OR haver_code LIKE 'PARETO_IPCA:automovel_usado%'
                                      OR haver_code LIKE 'PARETO_IPCA:alim_bebidas%';
  ```

---

## Bloco B — CPI-US (após release BLS ~09:30 BRT / 08:30 EST)

**Path recomendado** (passo-a-passo, 22 cats dashboard, ~2-3 min): segue `pareto_cpius/script_itau/RUNBOOK_ATUALIZACAO_DASHBOARD.md`. Mais debugável que o `update_cpius_lean.py` — CSVs intermediários inspecionáveis entre steps.

```powershell
cd pareto_cpius
$env:BLS_API_KEY   # confirma que existe

# 1. Fetch BLS (idx + pesos + custom aggs)
Rscript scripts/fetch_bls_cpiu.R                # ~60-90s
Rscript scripts/fetch_bls_pesos.R               # ~5-10s
Rscript scripts/build_custom_aggregations.R     # ~5s

# 2. Load 22 cats dashboard (18 base + 4 customs analíticos)
python script_itau/load_cpius_to_sql.py --only all_items,food,energy,core,core_goods,new_vehicles,used_cars_trucks,core_services,shelter,oer,rent,lodging_away,medical_care,transportation_services,airline_fares,car_truck_rental,core_ex_oer,cpi_ex_oer,core_services_ex_shelter,supercore_powell_old,super_super_core,core_services_ex_volatiles
```

**Path alternativo** (`update_cpius_lean.py`): self-contained, roda 1-2 cats rapidinho sem regerar tudo. Menos debugável mas mais rápido pra fix pontual (`python quick_update/update_cpius_lean.py --cats core_goods,oer`). Não usar como fluxo primário.

**Path full** (todas 49 cats × 3 datatypes = 147 séries): remove `--only` no step 2.

**Sanity checks pós-CPI-US**:
- Spot-check contra Haver: `core_services`, `core`, `food` — se divergir >0.3pp, olha memória `project_pareto_cpius_corp_vs_local_diverge.md` (issue conhecido, deferred 2026-08-21).
- Contagem esperada: 22 × 3 = 66 séries (path dashboard) ou 49 × 3 = 147 (path full).

### CPI-U difusão (mesmo padrão do PCE — novo hoje)

```bash
# Precisa BLS_API_KEY no env
python scripts/build_diffusion_cpius.py         # ~30-60s, ~53k rows raw + 5 séries
python script_itau/load_diffusion_to_sql.py --dry-run
python script_itau/load_diffusion_to_sql.py     # confirma [s/N]
```

- 5 séries: `CPIUS:diffusion_headline_yoy` + `_6ma_ann` + `_goods_yoy` + `_services_yoy` + `_goods_ex_cars_yoy`.
- Namespace disjunto: `CPIUS:diffusion_%` coexiste com as 49 séries idx CPI-U (`CPIUS:{cat}`).
- Values jul-2026 esperados: Goods 42.5%, Services 66.0%, Goods_ex_cars 43.1%, Headline 49.7%.
- Sem benchmark BBA pra CPI-U difusão — validação é sanity check de tendência (Services alto por shelter, Goods baixo por disinflação).

---

## Bloco C — PCE difusão (para apresentação)

**Não é release day de PCE** (PCE sai fim do mês). É pra apresentar o
protótipo pro chefe usando dados de julho/2026.

Se o corp já tem os dados carregados, mostra direto do dashboard. Se
não, roda a carga primeiro:

```bash
cd pareto_pce
export BEA_API_KEY=<sua_key>   # se não estiver no env
python scripts/fetch_pce.py             # ~30-60s
python scripts/build_diffusion.py       # <5s
python script_itau/load_pce_to_sql.py --dry-run
python script_itau/load_pce_to_sql.py
```

**Talking points pra apresentação**:
- Fonte: BEA U20404 (Underlying Detail, Price Indexes for PCE by Type of Product).
- Metodologia: contagem simples de leaves com YoY > 3% (mesmo critério do chart BBA).
- Cars: 6 items no nível de linha de produto — batem exato.
- **Goods 47.8% vs BBA 47.3% (Δ 0.5pp)** ✓
- **Services 58.0% vs BBA 57.8% (Δ 0.2pp)** ✓
- **Goods_ex_cars 50.0% vs BBA 49.4% (Δ 0.6pp)** ✓
- Bônus: Headline 6m annualized incluso (não estava no BBA).
- Gap remanescente 0.2-0.6pp: metodologia BBA usa lista analítica proprietária de items; nosso match >99% ainda assim.
- Dívida documentada: se quiser fechar exato, precisamos da nota interna Itaú BBA com a lista de leaves (registrado em `pareto_pce/TODO.md`).

---

## Se algo der errado

- **`load_pareto_to_sql.py` reclama de label faltando** (`automovel_novo` etc): CSV do R saiu com nome legado. Confirma `git pull` foi feito e o R rodou com script atualizado. Se persistir, patch temporário: renomeia manualmente no CSV (`sed 's/automovel_novo/auto_novo/g'`).
- **Preflight falha (coluna `bls_code` ausente)**: rodar `ALTER TABLE OPT_Macro_Series_2 ADD bls_code VARCHAR(255) NULL;`.
- **Difusão sobe lixo (10^50)**: `build_pareto_indice.R` não tem o special-case. Confirma commit `300f6e6` está no branch atual.
- **CPI-US spot-check diverge**: dívida conhecida (deferred 2026-08-21). Não bloqueia release.

## Ordem preferencial se atrasar

Se tudo atrasar, prioridade decrescente:
1. IPCA (mais visível internamente)
2. CPI-US (dashboards ativos dependem)
3. IPCA-15 (só se houve reprocessing pedido)
4. PCE (protótipo — pode apresentar da tela local se corp não subir)
